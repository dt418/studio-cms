---
description: Create or refresh the repository harness without platform-specific shell scripts.
---

Run `pnpm harness:init`, then report the generated `.harness/` artifacts. Use `--force` only when the user explicitly authorizes replacing existing harness state.
