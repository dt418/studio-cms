---
description: Select the harness role and model tier before working or delegating.
---

Run `pnpm harness:orchestrate -- --role planning --runtime claude --json`. For authentication, authorization, data migration, payment, or security work, run the reviewer with `--role reviewer --risk high`. Use the shared artifacts under `.harness/`; do not keep task state only in chat.
